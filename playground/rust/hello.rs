fn main()  // Define the main function.
{
    println!("Hello, World!");  // `println!` is a macro that prints a string
                                // with a newline at the end.
    /*
     * Using a `!` means that you are
     * calling a macro, and not a function.
     */
}
